[project]
name = "djangoproject"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2.2",
    "django-restframework>=0.0.1",
    "gunicorn>=23.0.0",
]
